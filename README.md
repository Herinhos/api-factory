# API Factory

Uma coisa ai

instale uv com winget:

``` cmd
winget install astral-sh.uv --source winget
```

não se esqueça de colocar o caminho no PATH

clone o repositorio aonde vc precisar

depois use o comando abaixo dentro da pasta do repositorio clonado

``` cmd
uv sync
```
